{% extends 'base.html.twig' %}

{% block body %}
{{ form_start(form) }}
    <div class="container">
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Сохранить') }}</button>
    </div>
{{ form_end(form) }}
{%endblock%}

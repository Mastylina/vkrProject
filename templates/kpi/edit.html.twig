{% extends 'base.html.twig' %}

{% block title %}Edit Kpi{% endblock %}

{% block body %}
    <div class="container">
    <h1>Изменить Kpi</h1>

    {{ include('kpi/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_kpi_index') }}">Вернуться</a>

    {{ include('kpi/_delete_form.html.twig') }}
    </div>
{% endblock %}

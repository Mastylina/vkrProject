{% extends 'base.html.twig' %}

{% block title %}Должность{% endblock %}

{% block body %}
    <div class="container">
    <h1>Должность</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ post.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_index') }}">Вернуться</a>

    <a href="{{ path('app_post_edit', {'id': post.id}) }}">Изменить</a>

    {{ include('post/_delete_form.html.twig') }}
    </div>
{% endblock %}
